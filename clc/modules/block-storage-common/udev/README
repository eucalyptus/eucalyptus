These are udev scripts to ensure that "eucalyptus" is able to access block devices attached via iSCSI (SAN).

check-iscsi-target-name       -> /usr/libexec/eucalyptus
55-eucalyptus-openiscsi.rules -> /usr/lib/udev/rules.d
